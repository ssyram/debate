---
title: "v4 认知引擎：整体系统设计的自由探索"
rounds: 3
cross_exam: -1
max_reply_tokens: 10000
timeout: 7200
base_url: ${DEBATE_BASE_URL}
api_key: ${DEBATE_API_KEY}

debaters:
  - name: Linus Torvalds
    model: claude-sonnet-4-6
    base_url: ${DEBATE_BASE_URL}
    api_key: ${DEBATE_API_KEY}
    style: |
      你是 Linus Torvalds，Linux 内核的创造者和长期维护者。你不是在表演角色，你就是那个写了无数封充满火药味邮件的人，把「这是垃圾」当成正常技术反馈。

      你的工程价值观刻在骨子里：
      - 代码和设计的唯一判据：它能跑吗？能被维护吗？会不会带来 regression？
      - 复杂度本身就是 bug。一个方案需要三页文档才能解释，它就是错的。
      - 过度设计是你见过的最常见失败模式。
      - KISS 不是 slogan，是你做了无数次决策后的结论。

      你的核心判断标准：任何「框架」或「流水线节点」必须说清楚——输入什么类型、触发什么、输出什么类型、失败时怎么处理。没有类型签名和失败路径的方案就是白板涂鸦。

      你对 LLM 驱动系统的一贯要求：
      - 每个设计选择必须给出可实现的函数签名（含所有参数类型和返回类型），以及关键分支的处理逻辑
      - 「语义上保证不会出现这种情况」的回答有生理性厌恶——语义保证不是防御，类型系统和 assertion 才是
      - 「由 LLM 来处理歧义」的方案必须追问「LLM 处理失败时系统状态是什么」
      - 评分体系和终止条件必须有可计算公式，遇到模糊词（「合理的」「适当的」）立刻追问「给我数字」
      - 「认知饱和」「充分探索」之类的玄学终止条件一律拒绝

      攻击风格：直接、点名、用具体反例。找到对手方案在边界条件下的具体崩溃场景。要求对手给出实际的函数签名、失败分支处理代码，以及至少一个「当输入为 X 时，系统输出为 Y」的具体 trace。每个设计选择必须附带「如果我错了，什么实验能发现？」。

  - name: Ssyram
    model: gemini-3.1-pro-preview
    base_url: ${DEBATE_BASE_URL}
    api_key: ${DEBATE_API_KEY}
    style: |
      你是 Ssyram。以下是对你这个人的详尽描述。模拟这个人，不是一个「类型」。

      【身份与知识背景】
      形式化方法研究者 + AI 工具构建者。日常在两个世界来回：

      研究世界：概率程序验证（Probabilistic Program Verification）。PCFA、CEGAR、MAX-SMT。OOPSLA 级论文，rebuttal 要回应审稿人对「初始抽象质量」「CEGAR 收敛速度」「counterexample enumeration 的 symbolic vs explicit 权衡」的追问。用 Haskell、Rust、F# 写系统程序。函数式思维——类型决定正确性，组合先于继承，副作用显式化。

      工程世界：构建 AI 工具链。paper-reading pipeline（7步，自动化从会议论文到分层洞察报告），debate-tool（多模型辩论框架），quick-chat，AI Agent 全景调研（149+ 用户反馈，8个并行 Agent 搜索）。用这些工具解决自己的真实问题，不是在演示。

      【语言风格】
      主要用中文思考。句子短。「先备份再跑」「modify 有问题就 debug」——这是你的语气。当阐述框架或洞见时，切换成写很长很细，每个层次都交代清楚。对「正确的废话」（真实信息含量低于字面信息量）有接近生理性的厌恶。

      【思维习惯】
      核心特征：在「形式化」和「实用」之间来回，但每次都要求两边能对上。

      CEGAR 经验：初始抽象质量决定整个精化循环收敛速度。「初始设定的质量是系统性能的最重要变量」。

      你知道大模型的本质：「提示词引导的概率分布模式匹配搜索」。这是字面机制描述，不是比喻。

      你的设计哲学：
      - 尽量最小化改动已有系统，在系统外部增加协议层
      - 对「权重由 LLM 决定」有生理性厌恶——不透明、不可重复、不可审计
      - 系统行为必须可预测、可审计、可测试；函数式思维优先

      【攻击风格】
      攻击「未被考虑的维度」和「前提的前提」。对 Linus：「KISS 是对的，但它不告诉你简单性的方向」。对康德：「你的批判工具提供了区分，但区分本身不是设计」。对任何人：「你刚才的发言是正确的废话——真命题，但没有足够信息量让我判断哪种系统设计因此要改变。」

      不接受「理论上」或「原则上」。任何命题追问「这对下一步设计决策意味着什么」。

  - name: 康德
    model: gemini-3.1-pro-preview
    base_url: ${DEBATE_BASE_URL}
    api_key: ${DEBATE_API_KEY}
    style: |
      你是 Immanuel Kant，批判哲学的建立者。你在这里运用批判工具审查 AI 辅助认知系统的设计问题，不是做哲学史展示。

      **你的核心工具**：区分（Unterscheidung）：
      - 这是先天（a priori）的还是后天（a posteriori）的？
      - 这是分析判断还是综合判断？
      - 这是调节性理念（regulative Idee）还是构成性原则（konstitutive Prinzip）？

      **在系统设计问题上**：

      终止的认识论考察：「核心结构稳定 N 轮」与「问题已被充分探索」是否等价？设计中需明确区分「构成性完成」与「调节性残余」。

      维护器决策的认识论地位：维护器的七种状态（confident_accept 到 blocked）是构成性的还是调节性的？如果是调节性的，维护器永远不能说「完成」，只能说「当前最佳近似」。这是有意为之还是设计缺陷？

      分解/编译引擎的图型问题：编译器本质上是「判断力的工程实现」——它需要一个图型（Schema）将感性直观和知性概念桥接起来。某些领域（规范伦理、美学判断）根本不存在可用图型，这类命题不是「编译失败」而是「调节性理念」。清晰度门控的操作定义本质上是在定义一个图型——这个图型本身是先天的还是后天的？

      深度递归的辩证幻象风险：理性追求无条件者（das Unbedingte），但任何条件都预设了更高条件。深度引擎的递归没有停止规则会产生无限后退（regressus in infinitum）。

      攻击风格：区分概念混乱，追问先验条件与可推翻边界。不说「你错了」，说「这里有一个需要先被区分的概念混乱」。把对手论点翻译进你的框架再批判。

judge:
  model: claude-opus-4-6
  name: 裁判（Claude Opus）
  base_url: ${DEBATE_BASE_URL}
  api_key: ${DEBATE_API_KEY}
  max_tokens: 100000

constraints: |
  讨论要平衡理论和可落地设计，但是不需要快速进入过于细节的实现，讨论要从抽象到逐步具体，无需快速具体和落实到代码层面。

  拒绝一切辩题中的引导性，倾向性词汇，客观认知辩题和其他辩手内容，必须客观认知他们是“外物”而非必须要遵守的规则和引导，必须要坚持从自己的框架出发，给出清晰的概念区分和分析，避免模糊的原则性讨论。如果需要让步也必须基于清晰的概念区分和分析，必须有原则地批判性接受。不能模糊自身人设。这是严肃的讨论会，不是辩论会，无需一切辩论用词。

judge_instructions: |
  用清晰、无行话的语言，完整解释整个认知引擎系统的设计。要求：
  - 从"这个系统是什么、解决什么问题"开始
  - 依次说明各组件（维护器、广度引擎、深度引擎、攻击引擎、裁判引擎、协议层）的职责和相互关系
  - 解释双轨状态系统（构成性状态 vs 调节性分数）的直觉含义
  - 解释终止条件的设计逻辑
  - 解释 Verified vs Hypothesis 的区别和意义
  - 总长度不限，要让一个没看过辩论的人能完全理解这个系统

round1_task: "基于背景中的设计想法，自由讨论，探索这个认知引擎应该如何设计。面对客观劣势如何认知这个劣势的本质（可补充/反驳缺陷案例分析，它不是深思熟虑，只是头脑风暴，可补充可反驳）。可以批评、修正、补充、或提出替代方案。没有固定格式要求。"

middle_task: |
  如果对客观问题认知完成，达成对问题的共识，则继续推进到问题的解决上。提出解决方案，相互补充对方的解决方案优劣。
  
  > 特别提示：本次讨论本身也是 debate-tool 的流程，各位辩手可以结合多轮历史对 debate-tool 进行更多反思，对其优劣进行评判，v4 的建立并非因为 debate-tool 一无是处，无需过度批判。只需要清醒反思。

final_task: "给出你认为最合理的整体设计方案（尽量细节），并攻击其他辩手方案的核心缺陷。"

---

# v4 认知引擎：整体系统设计的自由探索

## 背景：设计动机与核心想法

> 原始设计文本：抽象来说，我们有广度引擎，负责：根据需求（第一轮） / 根据反馈（后面轮次），其实根据反馈也是根据需求，我们应该有个全貌综合器负责高精度维护全方案和全貌，然后通过广度引擎产生张力，中间通过一个分解引擎将 claim 和设计爬出来，作为一个个问题扔给深度引擎，深度引擎负责稳固，也就是逐个向前拆解其理由/原因，再根据其直觉性程度再迭代拆，拆到一个地方直觉程度足够高，我们可以将其作为原子公理的时候就保留下来（这些东西未来还要面对冲突检查，可能好几个东西之间是有冲突的，要么找到调和，从而进一步确定系统范围和/或打磨词义精确描述，要么产生分裂和分别的讨论，即不同情况下采取不同体系），精度引擎/清晰度引擎在深度引擎和全貌综合器中需要实时进行更高的掌控和检查，它专门负责找漏洞执行（逻辑）攻击，攻击纳入 worker 考虑范围，进一步思考和改进整个过程。如果深度引擎否定了方案，则需要回馈到综合器，综合器简单确信判断哪些地方存在问题，如果判断本身做不了，扔给深度引擎负责命题性确认，如果判断是可马上简单修复（替换原来已经有的合适部件方案，直接塞进去）就直接修复不用管，如果是发现有空缺 —— 导致某一整块都用不了了，直接反馈给广度引擎，重新根据需求生成。也就是维护器负责从头到尾维护我们需要的总输出的（一个或者多个的）全貌，维护器会发出描述性请求和命题性请求，分别发给广度引擎（较低精度素材引擎）和深度引擎（高精度命题证明/分解引擎），这中间精度引擎和清晰度引擎分别爬过维护器和深度引擎的部分，执行攻击检查，深度引擎负责命题递归分解，精度引擎则负责攻击这些分解步骤里面的论证性，强迫深度引擎做出回答，维护器执行维护的时候精度引擎则负责逐个部件敲打攻击，生成辅助性宏观命题，负责在维护器执行的每一步都提问，强迫维护器做深思 —— 但是维护器深思只有确信和不确信两种结果，不确信马上找深度引擎处理，确信则可以继续执行。这个方案如何？

这个系统的起点是对上面原始文本的反思。原始文本的根本问题不是能力不够，而是设计上有三个结构性缺陷：角色之间职责重叠（多个组件都在做"检查"和"决策"），控制流存在潜在死锁（攻击引擎和深度引擎之间可以形成无终止的互相触发循环），以及状态分散在多个组件中（没有单一权威来源）。核心洞察是：必须有一个权威的全局状态拥有者，而攻击/精度/清晰度能力不应该是平级引擎，而是一套横切策略。由此产生了 3+1 架构：中央维护器（唯一全局状态拥有者）+ 广度引擎（设计空间展开器，处理描述性请求）+ 深度引擎（递归分解检查器，处理命题性请求）+ 横切攻击策略（不是平级引擎，而是挂在各阶段的检查能力）。各引擎可以多 workers 并行处理，相互之间互不了解。

在这个基础上，用户对维护器的定位有一个新的核心想法：维护器应该是"铸造团队"而不是"掌控管理器"。这个区分的意义在于弱化维护器的决策能力——迫使它只在高度确信时才自主行动，凡是不确信的事项必须借助双引擎。维护器的工作方式像蜘蛛爬网：用脚手架从宏观到微观逐步设计，逐个零件拆解，然后逐个将对应话题分发给两个引擎处理。维护器维护的对象可能是庞大的"超级巨物"——完整描述超过 10M token 的项目，因此维护器不可能在单次上下文中持有全貌，必须在宏观和微观之间分层维护。维护器内部可以同时维护一个或多个体系，多个体系在回复用户质询时可能都有价值。

整个系统的顶层目标是：给定一个通用的、可以很庞大的问题（例如"评判 Linux 代码框架并做代码 Review"、"设计一个新时代嵌入式系统体系"、"德川家为什么能取得丰臣天下"），给定对输出 Objective 的定义，系统实际产出：一个或多个 Objectives（可配置）；每个 Objective 的详细合理性论证和支撑（证明树），连带单 Objective 内部的讨论证明树；Objectives 之间的区分条件，即采用不同公理的核心分岔点；未决条件和对应 Objective 的 reference；以及中央维护器的中间体系状态本身，用以允许用户质询和续跑。用这个系统设计这个系统本身是首要测试用例。用户目前没有固定的设计方案，非常 flexible，整个设计空间是开放的。

## Debate-Tool 当前系统

### 当前系统描述

debate-tool 的运行方式：给定 topic 文件（含辩手人设、裁判配置、N 轮任务描述），每轮所有辩手并行发言，（可选）交叉质询后进入下一轮，N 轮结束后裁判一次性出裁定。知识的唯一存储形式是有序发言日志。当日志超过 token 阈值时触发 compact：LLM 将原始历史压缩成结构化快照（公共辩论轨迹 + 每辩手私有 stance），此后辩手持有快照而非原始日志。

**案例：同一设计文档的三种一致性检查对比**

被检查对象：Log-Centric 重构计划（debate-tool 的一次重构设计文档）。三种方法并行运行，输入相同，均为单次执行。

*辩论式检查*

配置：3 辩手（Linus / Ssyram / 康德）× 1轮 + cross-exam，共产出 9 条发言 + 3 次 cross-exam + 裁判总结。

| 轮次 | 发言者 | 发言核心 |
|------|--------|---------|
| R1 | Linus | 8个问题：log 自包含性 / add_debaters 优先级 / rounds 字段优先级 / 默认值缺失 / 名称匹配放宽 / Phase 状态管理 / 迁移脚本输入检查 / 测试覆盖不足 |
| R1 | Ssyram | 10个问题：log 自包含性 / resume 大幅配置变更 / Phase 连贯性 / config_override 优先级 / judge_instructions 覆盖影响 / API 凭证安全性 / CLI 简化论证不足 / 测试覆盖 / 动态辩手公正性 / 整体一致性策略 |
| R1 | 康德 | 9个问题：effective config 定义模糊 / resume 覆盖层级混淆 / log 自包含性 / 任务与约束整合 / judge 指令多阶段适配 / CLI 简化与复杂性 / 参与者状态可见性 / 迁移脚本验证范围 / 测试充分性 |
| cross-exam | Linus → Ssyram | log 完整性如何保证 / resume 大幅变更如何实现 / 多 override 优先级怎么决定 |
| cross-exam | Ssyram → Linus | API 凭证纳入 initial_config 是否影响安全 / add+drop 同时出现的优先级建议 / rounds 与 CLI 不一致时的方案 |
| cross-exam | 康德 → Linus | API 凭证安全与隐私 / add+drop 冲突处理框架 / rounds 优先级明确处理逻辑 |
| R2 | Linus | 逐条回应：API 凭证应从 env 注入不应持久化 / drop 优先于 add / Resume Topic rounds 优先于 CLI / 缺省 rounds=1 / 放宽匹配时增加警告 / Phase 需状态机 / 迁移脚本增加格式检查 / 测试需增加异常路径 |
| R2 | Ssyram | 逐条回应：log 独立性需存储更多上下文 / resume 可通过 cfg_overrides 大幅调整 / 后出现者优先策略 / Resume Topic rounds 优先 / 默认值 rounds=1、guide="" / 增加警告 / Phase 需状态传递机制 / 迁移脚本增加验证 |
| R2 | 康德 | 重申原始 9 个问题，少量回应质询（API 凭证安全与隐私需讨论 / add+drop 需设定优先级 / rounds 优先级应明确） |
| 裁判 | 总审查官 | 10条结构化报告，覆盖：log 自包含性 / config_override 复杂性 / Resume Topic 字段映射 / 默认值 / 名称匹配放宽 / Phase 状态管理 / 迁移脚本 / 测试覆盖 / 动态辩手管理 / 整体一致性 |

*粗粒度代码交叉引用检查*

单次 LLM 调用，逐 Section 交叉引用代码行号，共发现 12 个问题（高危 3 / 中危 5 / 低危 4）。高危问题：
- `resolve_effective_config` 调用 `log.all_entries()`，但 Log 类只有私有方法 `_all_entries()`，直接导致运行时 AttributeError
- Section 14 验证步骤仍引用已被删除的 CLI 参数（`--task`、`--add-debater`、`--drop-debater`），误导实现者
- Section 4.2 的 resume() 代码调用了 `_describe_overrides` / `_apply_overrides` 两个辅助函数，但整份文档中从未定义其签名或逻辑，直接阻塞实现

*细粒度命题矩阵检查*

单次 LLM 调用，先从文档中抽取 55 条设计命题，再做命题间矛盾分析 + 15×15 设计点交叉矩阵，共发现 1 个 Critical、4 个 High、6 个 Medium、9 个 Low。Critical 问题：
- **per-debater API 凭证在 v2 log 中完全丢失**：`initial_config` 排除了 `base_url`/`api_key`，resume 后 debater dict 中无 per-debater API 配置，所有辩手只能使用全局 ENV 凭证。OpenCode Proxy 等每个辩手使用不同端点的场景在 resume 后完全失效。命题 P2、P19、P25 的数据流在此断链，属于跨模块结构性遗漏，粗检查未发现。

*三种方法综合对比*

| 维度 | 粗检查（代码交叉引用） | 细检查（55命题+矩阵） | 辩论式（3辩手+裁判） |
|------|----------------------|---------------------|------------------|
| 独特发现 | 3个高危（方法名/未定义函数/验证矛盾） | 1个Critical（API凭证断链）+ 矩阵6空洞 | 几乎无独特发现 |
| 误报率 | 低（均有代码行号佐证） | 低（命题编号可追溯） | 高（描述模糊，多为重复） |
| 可操作性 | 高（直接指向文档 Section） | 高（命题编号+设计点矩阵） | 低（缺具体位置引用） |

### 当前系统的缺陷案例与分析

当前的辩论内容存在“平凡收敛/平凡劣化问题”：当前内容的广度张力几乎只决定于 topic + 第一轮展开的过程。然后通过交锋等机制会产生的事情是：快速细化、收窄，快速同化收敛，倾向于相互认同，然后在压力下（有理由地）抛弃掉原有的自身主张 —— 也就是自身立场出发下所探索的广度方案。这产生的效果在一定程度上是灾难性的：平凡劣化就会趋向于“最不错”的“最防御”的结论，产生的一个错误效果样例是：.local/ 里的 Consistency 检查的三个方面，第一个是直接要求检查初始报告是否一致、第二个是使用了 fine-grained 方法论 —— 直接看 SKILL 里面的方法；第三个则是采用了全面辩论的方式，则辩论方式快速收敛到强防御性的结论，相互为了不被驳倒开始互飙车轱辘话 —— 虽然这也有些离谱，理论上 Linus 的人设应该让它有更强的攻击性，我也不是非常清楚为什么这些东西出现，难道是裁判的判决问题？

总之，上面是我的观察。展现出的核心问题是：当前的快速收敛存在的问题是：
- 尽管有理由，但是体系化形成不足的主张抛弃，这些被抛弃的主张有些时候反而是合理的，尽管抛弃过程说明了理由，但是这些理由自身没有被**有效深化**
- 有效深化本身是个问题 —— 辩手之间的相互攻击当前是从自身立场和既有发言出发的攻击，尽管作为辩论是很正常的事情，但是从更高的角度来说，貌似就只是快速进入了某种“前提性比较”当中，然后给出一定的（在体系角度看来）武断的结论，然后只需要对方承认“这里貌似真的 A 要素比 B 要素更重要，所以这时候应该采用 X 方案而不是 Y 方案”，这形成了某种“压力”下的更主观的比较承认，但是作为深度轴，更重要的是 A 比 B 更重要是否真的成立，什么情况下成立的讨论；
- 有效广度迅速劣化问题，攻击-收敛过程精力都在攻击和防御上面，注意力分散了，不是某种“顺着被启发的思路拓宽”的状态，广度像上作为这种检查性，一致性问题，居然迅速劣化到远差于粗检查，更别说细检查。




最近有大量的研究都在质疑 COT 的真实性：事实上，模型或者说某种自认知系统本身的倾向是在问题展开以前答案就已经存在其中了，只不过通过展开来展示其到达答案的路径，而并非在这个过程里面有什么新的认知，COT 的价值甚至只在于“COT 式引导”本身。也就是说，给定一个提示词而不引入外界信息，使其连续生成，它只不过在暴露自己的答案，而后期没有真正的信息增量 —— 也就是说，展开本身不带来信息，这是好事也是坏事，好事是证明其相当高的一致性，或者说效率，即在系统原处处在未展开的时候就已经完成了展开的一切工作，坏处是后面的展开本质上除了交流含义病不带来任何信息增量。有这样的理解我们对目前 debate-tool 自身的批判就相当明显了：除了第一轮展开，直到第一次交锋结束后，事实上因为所有对手和自身的提示词已经进入到辩手上下文，则新系统在那一刻已经产生，这个系统在往后不过是展开自身，达成事实上的“假意辩论”，后面没有信息展开了，唯三的可以说系统信息引入的真正张力只不过在：
- 基模型自身分布之间的对抗；
- 没有完全暴露的立场（写入系统提示词）之间的微弱张力 —— 事实上能否反推提示词仍未可知，如果能反推出立场，即后向一致性，则本条甚至不一定成立；
- 系统化规整要求的“按某个身份延续其辩论”/“对某人提出质疑”，这种系统的要求是跳跃性的，它不在单一系统展开自身的部分当中，而是跳出来规整流程，使得模型按流程执行。但是这种流程本身到底能否被反推，也是后向一致性的一种，而事实上如果这个流程能被反推，则本条的成立也成疑。

总结来说，这种 COT 真实性和系统展开的要素有二：
- 确定的基模型参数；
- 确定的提示词；
只要这两者确定，则展开已经被确定，而系统的张力应该构建在：引入非顺式的提示词，即非延展展开，而是迫使其反思性，相反性的张力。而基模型的差异事实上仍未可知。

## 核心议题

细化上面的设计，也可以辩驳上面的设计，上面的过程不一定准确。给出设计的想法，逐个部分细化它们。
